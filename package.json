{
  "name": "xyntax",
  "version": "1.0.0",
  "description": "Lightweight JavaScript syntax highlighter with zero performance overhead",
  "main": "index.js",
  "bin": {
    "xyntax": "bin/cli.js"
  },
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postversion": "git push && git push --tags"
  },
  "author": "Bismark Yamoah",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0"
  }
}
