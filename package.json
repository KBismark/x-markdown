{
  "name": "xyntax",
  "version": "1.0.0",
  "description": "JavaScript syntax highlighter with zero performance overhead",
  "main": "dist/lib/index.js",
  "bin": {
    "xyntax": "bin/cli.js"
  },
  "directories": {
    "lib": "dist/lib"
  },
  "scripts": {
    "test": "tsc --outDir dist/ && node tests/test_returner.js",
    "build": "tsc --outDir dist/",
    "postversion": "git push && git push --tags"
  },
  "author": "Bismark Yamoah",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2"
  },
  "files": [
    "bin/",
    "dist/"
  ]
}
